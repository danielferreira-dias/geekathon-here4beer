# AWS configuration for Bedrock
# Region
AWS_REGION=us-east-1

# Credentials (use environment or ~/.aws/credentials in prod; .env is for local dev only)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=

# Bedrock model id (Inference Profile ARN recommended)
# BEDROCK_MODEL_ID=arn:aws:bedrock:us-east-1:816308070251:inference-profile/us.anthropic.claude-sonnet-4-20250514-v1:0

# Optional generation config
# MAX_TOKENS=1500
# TEMPERATURE=0.2
# TOP_P=0.9
# TOP_K=250
# BEDROCK_LATENCY=standard  # Ignored (performanceConfig not supported by current SDK)

# Uvicorn / server
PORT=8000
